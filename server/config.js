module.exports = {
  spawnWaveInterval: 30 * 1000, // 30 seconds each creep wave
  spawnInterval: 2 * 1000,      // 2 seconds each creep inside the wave
  spawnPerWave: 3,              // number of creeps to spawn per wave
}
